@model IEnumerable<RealEstateManagement.Models.Contract>

@{
	ViewData["Title"] = "Contracts";
}

<h1>Contracts</h1>

<p>
	<a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
	<thead>
		<tr>
			<th>Type</th>
			<th>Date</th>
			<th>Amount</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
	@if (Model != null) {
		foreach (var item in Model)
		{
			<tr>
				<td>@item.ContractType</td>
				<td>@item.ContractDate.ToShortDateString()</td>
				<td>@item.Amount</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.Contract_ID">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.Contract_ID">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.Contract_ID">Delete</a>
				</td>
			</tr>
		}
	}
	</tbody>
</table>
