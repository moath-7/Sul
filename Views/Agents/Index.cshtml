@model IEnumerable<RealEstateManagement.Models.Agent>

@{
	ViewData["Title"] = "Agents";
}

<h1>Agents</h1>

<p>
	<a asp-action="Create" class="btn btn-primary">Create New</a>
</p>

<table class="table table-striped">
	<thead>
		<tr>
			<th>Name</th>
			<th>Phone</th>
			<th>Region</th>
			<th></th>
		</tr>
	</thead>
	<tbody>
	@if (Model != null) {
		foreach (var item in Model)
		{
			<tr>
				<td>@item.Name</td>
				<td>@item.PhoneNumber</td>
				<td>@item.RegionCovered</td>
				<td>
					<a asp-action="Edit" asp-route-id="@item.Agent_ID">Edit</a> |
					<a asp-action="Details" asp-route-id="@item.Agent_ID">Details</a> |
					<a asp-action="Delete" asp-route-id="@item.Agent_ID">Delete</a>
				</td>
			</tr>
		}
	}
	</tbody>
</table>
